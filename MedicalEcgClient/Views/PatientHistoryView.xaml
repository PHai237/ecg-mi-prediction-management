<UserControl x:Class="MedicalEcgClient.Views.PatientHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:MedicalEcgClient.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:PatientHistoryViewModel}">

    <Grid Background="#F5F5F5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" Margin="10,10,5,10" CornerRadius="5" Padding="15">
            <Border.Effect>
                <DropShadowEffect Color="#DDD" ShadowDepth="2" BlurRadius="5" Opacity="0.5"/>
            </Border.Effect>
            <StackPanel>
                <Button Command="{Binding GoBackCommand}" HorizontalAlignment="Left" Background="Transparent" BorderThickness="0" Margin="-10,-10,0,10">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE72B;" FontFamily="Segoe MDL2 Assets" Margin="0,0,5,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Quay lại danh sách"/>
                    </StackPanel>
                </Button>

                <Border Width="100" Height="100" CornerRadius="50" Background="#F0F2F5" Margin="0,0,0,20" BorderBrush="#E0E0E0" BorderThickness="1">
                    <TextBlock Text="&#xE77B;" FontFamily="Segoe MDL2 Assets" FontSize="40" Foreground="#AAA" 
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <TextBlock Text="{Binding CurrentPatient.FullName}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>
                <TextBlock Text="{Binding CurrentPatient.PatientCode}" FontSize="14" Foreground="Gray" HorizontalAlignment="Center" Margin="0,5,0,20"/>

                <Grid Margin="0,5">
                    <TextBlock Text="Năm sinh:" Foreground="Gray"/>
                    <TextBlock Text="{Binding CurrentPatient.DateOfBirth, StringFormat='dd/MM/yyyy'}" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                </Grid>
                <Grid Margin="0,5">
                    <TextBlock Text="Giới tính:" Foreground="Gray"/>
                    <TextBlock Text="{Binding CurrentPatient.Gender}" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                </Grid>

                <StackPanel Margin="0,15,0,5">
                    <TextBlock Text="⚠ LƯU Ý / GHI CHÚ:" Foreground="#D32F2F" FontWeight="Bold" FontSize="12"/>
                    <TextBlock Text="{Binding CurrentPatient.Note}" TextWrapping="Wrap" Foreground="#333" FontStyle="Italic" Margin="0,5,0,0" Padding="5" Background="#FFF8E1"/>
                </StackPanel>

                <Separator Margin="0,20"/>

                <TextBlock Text="TẠO CA KHÁM MỚI" FontWeight="Bold" Foreground="#0D6EFD" Margin="0,0,0,10"/>

                <Button Command="{Binding CreateEcgCaseCommand}" Height="50" Margin="0,0,0,10" Background="#0D6EFD" Foreground="White" BorderThickness="0" FontWeight="Bold">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE9D2;" FontFamily="Segoe MDL2 Assets" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock Text="ĐO ĐIỆN TIM (ECG)" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Command="{Binding CreateImageCaseCommand}" Height="50" Background="#6C757D" Foreground="White" BorderThickness="0" FontWeight="Bold">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE722;" FontFamily="Segoe MDL2 Assets" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock Text="CHỤP ẢNH / SCAN" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" Background="White" Margin="5,10,10,10" CornerRadius="5" Padding="15">
            <Border.Effect>
                <DropShadowEffect Color="#DDD" ShadowDepth="2" BlurRadius="5" Opacity="0.5"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="LỊCH SỬ KHÁM BỆNH (CASES)" FontSize="18" FontWeight="Bold" Foreground="#333" Margin="0,0,0,15"/>

                <ListView Grid.Row="1" ItemsSource="{Binding Cases}" SelectedItem="{Binding SelectedCase}" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Margin" Value="0,0,0,10"/>
                            <Setter Property="Padding" Value="10"/>
                            <Setter Property="Background" Value="#FAFAFA"/>
                            <Setter Property="BorderBrush" Value="#EEEEEE"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <EventSetter Event="MouseDoubleClick" Handler="OnHistoryItemDoubleClick"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Border Width="40" Height="40" CornerRadius="20" Background="#E3F2FD" Margin="0,0,15,0">
                                    <TextBlock Text="&#xF0E3;" FontFamily="Segoe MDL2 Assets" FontSize="20" Foreground="#0D6EFD" 
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Note, TargetNullValue='(Không có ghi chú)', FallbackValue='Ca Khám'}" FontWeight="Bold" FontSize="14" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Margin="0,2,0,0">
                                        <Run Text="Trạng thái:" Foreground="Gray"/>
                                        <Run Text="{Binding Status}" FontWeight="SemiBold" Foreground="#2E7D32"/>
                                        <Run Text=" | " Foreground="LightGray"/>
                                        <Run Text="Số ảnh:" Foreground="Gray"/>
                                        <Run Text="{Binding ImageCount}" FontWeight="SemiBold"/>
                                    </TextBlock>
                                </StackPanel>

                                <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                                    <TextBlock Text="{Binding MeasuredAt, StringFormat='dd/MM/yyyy'}" FontWeight="Bold" HorizontalAlignment="Right"/>
                                    <TextBlock Text="{Binding MeasuredAt, StringFormat='HH:mm'}" Foreground="Gray" HorizontalAlignment="Right"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <Grid Grid.Row="1" Background="#88FFFFFF" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="&#xE768;" FontFamily="Segoe MDL2 Assets" FontSize="30" Foreground="#0D6EFD" HorizontalAlignment="Center"/>
                        <TextBlock Text="Đang xử lý..." Foreground="#0D6EFD" FontWeight="Bold" Margin="0,5,0,0"/>
                    </StackPanel>
                </Grid>

                <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center"
                            Visibility="{Binding Cases.Count, Converter={StaticResource BoolToVis}, ConverterParameter=Inverse}">
                    <TextBlock Text="&#xE7C5;" FontFamily="Segoe MDL2 Assets" FontSize="40" Foreground="#DDD" HorizontalAlignment="Center"/>
                    <TextBlock Text="Chưa có lịch sử khám bệnh" Foreground="Gray" Margin="0,10,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.ColumnSpan="2" Background="#CC000000" Visibility="{Binding IsViewingDetail, Converter={StaticResource BoolToVis}}">
            <Border Background="White" Margin="50" CornerRadius="8" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock FontSize="20" FontWeight="Bold" Foreground="#0D6EFD">
                                <Run Text="KẾT QUẢ KHÁM NGÀY: "/>
                                <Run Text="{Binding CurrentCaseDetail.MeasuredAt, StringFormat='dd/MM/yyyy HH:mm'}"/>
                            </TextBlock>

                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                <TextBlock Text="Mã HS:" Foreground="Gray"/>
                                <TextBlock Text="{Binding CurrentCaseDetail.PatientCode}" FontWeight="Bold" Margin="5,0,15,0"/>

                                <TextBlock Text="Trạng thái:" Foreground="Gray"/>
                                <TextBlock Text="{Binding CurrentCaseDetail.Status}" FontWeight="Bold" Foreground="Green" Margin="5,0,0,0"/>
                            </StackPanel>
                            <TextBlock Text="{Binding CurrentCaseDetail.Note}" FontStyle="Italic" Margin="0,5,0,0" TextWrapping="Wrap"/>
                        </StackPanel>

                        <Button Command="{Binding CloseDetailCommand}" HorizontalAlignment="Right" VerticalAlignment="Top" 
                                Background="Transparent" BorderThickness="0" Padding="10">
                            <TextBlock Text="&#xE711;" FontFamily="Segoe MDL2 Assets" FontSize="20"/>
                        </Button>
                    </Grid>

                    <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding CurrentCaseDetail.Images}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="0,0,0,20" BorderBrush="#EEE" BorderThickness="1" Padding="5">
                                        <StackPanel>
                                            <Image Source="{Binding Url}" Stretch="Uniform" MaxHeight="600"/>
                                            <TextBlock Text="{Binding OriginalFileName}" HorizontalAlignment="Center" Foreground="Gray" Margin="0,5,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <TextBlock Grid.Row="1" Text="Không có hình ảnh nào trong ca này." 
                               Foreground="Gray" HorizontalAlignment="Center" VerticalAlignment="Center"
                               Visibility="{Binding CurrentCaseDetail.Images.Count, Converter={StaticResource BoolToVis}, ConverterParameter=Inverse, FallbackValue=Collapsed}"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
<UserControl x:Class="MedicalEcgClient.Views.EcgMonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:scott="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
             xmlns:vm="clr-namespace:MedicalEcgClient.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DataContext="{d:DesignInstance Type=vm:EcgMonitorViewModel}">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#E3F2FD" Padding="15,10" BorderBrush="#BBDEFB" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Command="{Binding GoBackCommand}" Background="Transparent" BorderThickness="0" Foreground="#0D6EFD" Cursor="Hand"
                        IsEnabled="{Binding IsUploading, Converter={StaticResource BoolToVis}, ConverterParameter=Inverse}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE72B;" FontFamily="Segoe MDL2 Assets" Margin="0,0,5,0" VerticalAlignment="Center" FontWeight="Bold"/>
                        <TextBlock Text="Quay lại danh sách" FontWeight="Bold"/>
                    </StackPanel>
                </Button>

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="Bệnh nhân:" Foreground="Gray" Margin="0,0,5,0"/>
                    <TextBlock Text="{Binding CurrentPatient.FullName}" FontWeight="Bold" FontSize="16" Foreground="#0D6EFD" Margin="0,0,15,0"/>
                    <TextBlock Text="Mã HS:" Foreground="Gray" Margin="0,0,5,0"/>
                    <TextBlock Text="{Binding CurrentPatient.PatientCode}" FontWeight="SemiBold" Margin="0,0,15,0"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="&#xEC06;" FontFamily="Segoe MDL2 Assets" Foreground="Red" FontSize="18" Margin="0,0,5,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding HeartRate, StringFormat='HR: {0} BPM'}" FontSize="18" FontWeight="Bold" Foreground="Red"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" BorderBrush="#333" BorderThickness="1" Margin="10" Background="Black">
            <Grid>
                <scott:WpfPlot x:Name="EcgPlot" />

                <Grid Background="#CCFFFFFF" Visibility="{Binding IsUploading, Converter={StaticResource BoolToVis}}">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">
                        <TextBlock Text="&#xE896;" FontFamily="Segoe MDL2 Assets" FontSize="40" Foreground="#0D6EFD" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="{Binding StatusMessage}" HorizontalAlignment="Center" FontWeight="SemiBold" Foreground="#0D6EFD" Margin="0,0,0,5"/>
                        <ProgressBar Value="{Binding UploadProgress}" Maximum="100" Height="10" Foreground="#0D6EFD" Background="#E0E0E0" BorderThickness="0"/>
                        <TextBlock Text="{Binding UploadProgress, StringFormat='{}{0:0}%'}" HorizontalAlignment="Center" Foreground="Gray" Margin="0,5,0,0"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Foreground="Gray" FontStyle="Italic"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">

                <Button Command="{Binding SaveMeasurementCommand}" 
                        IsEnabled="{Binding IsUploading, Converter={StaticResource BoolToVis}, ConverterParameter=Inverse}"
                        Background="#0D6EFD" Foreground="White" FontWeight="Bold" FontSize="14"
                        Width="180" Height="50" Margin="10,0,0,0" BorderThickness="0" Cursor="Hand">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE898;" FontFamily="Segoe MDL2 Assets" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock Text="LƯU KẾT QUẢ"/>
                    </StackPanel>
                </Button>

                <Button Command="{Binding ToggleRecordingCommand}" 
                        IsEnabled="{Binding IsUploading, Converter={StaticResource BoolToVis}, ConverterParameter=Inverse}"
                        Height="50" Width="180" Margin="10,0,0,0"
                        Background="{Binding IsRecording, Converter={StaticResource BooleanToRedConverter}}" 
                        Foreground="Black" FontSize="14" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Content">
                                <Setter.Value>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="&#xE7C8;" FontFamily="Segoe MDL2 Assets" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                        <TextBlock Text="BẮT ĐẦU ĐO"/>
                                    </StackPanel>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsRecording}" Value="True">
                                    <Setter Property="Content">
                                        <Setter.Value>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="&#xE71A;" FontFamily="Segoe MDL2 Assets" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                                <TextBlock Text="DỪNG &amp; XEM"/>
                                            </StackPanel>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
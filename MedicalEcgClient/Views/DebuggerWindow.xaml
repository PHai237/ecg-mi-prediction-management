<Window x:Class="MedicalEcgClient.Views.DebuggerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:MedicalEcgClient.ViewModels"
        mc:Ignorable="d"
        Title="🛠 DEBUGGER &amp; GOD MODE (Ctrl+F12)" Height="600" Width="900"
        Background="#1E1E1E" WindowStyle="ToolWindow" Topmost="True">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                <TextBlock Text="SYSTEM LOGS" Foreground="White" FontWeight="Bold" VerticalAlignment="Center"/>
                <Button Command="{Binding ClearLogsCommand}" Content="Clear" Margin="10,0" Padding="5,2" Background="#333" Foreground="White"/>
                <Button Command="{Binding ForceGCCommand}" Content="Force GC" Padding="5,2" Background="#333" Foreground="White"/>
            </StackPanel>

            <ListBox Grid.Row="1" ItemsSource="{Binding LiveLogs}" Background="Black" BorderBrush="#333" 
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Timestamp}" Foreground="Gray" FontSize="11"/>
                            <TextBlock Grid.Column="1" Text="{Binding Level}" Foreground="{Binding Color}" FontWeight="Bold" FontSize="11"/>
                            <TextBlock Grid.Column="2" Text="{Binding Message}" Foreground="{Binding Color}" TextWrapping="Wrap" FontFamily="Consolas"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <Border Grid.Column="1" Background="#252526" BorderBrush="#333" BorderThickness="1,0,0,0" Margin="5">
            <StackPanel Margin="10">
                <TextBlock Text="HOT CONFIGURATION" Foreground="#007ACC" FontWeight="Bold" Margin="0,0,0,15"/>

                <TextBlock Text="Server URL" Foreground="#AAA"/>
                <TextBox Text="{Binding Config.ServerUrl}" Background="#333" Foreground="White" Margin="0,0,0,10"/>

                <TextBlock Text="COM Port" Foreground="#AAA"/>
                <TextBox Text="{Binding Config.ComPort}" Background="#333" Foreground="White" Margin="0,0,0,10"/>

                <CheckBox IsChecked="{Binding Config.IsSimulationMode}" Content="Simulation Mode" Foreground="White" Margin="0,0,0,10"/>
                <CheckBox IsChecked="{Binding Config.EnableRawLogging}" Content="Enable Raw Trace" Foreground="White" Margin="0,0,0,10"/>

                <Button Command="{Binding SaveConfigHotCommand}" Content="💾 SAVE HOT CONFIG" 
                        Background="#0D6EFD" Foreground="White" FontWeight="Bold" Padding="10" Margin="0,20,0,0"/>

                <TextBlock Text="Note: Một số thay đổi cần restart app." Foreground="Gray" FontStyle="Italic" TextWrapping="Wrap" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>